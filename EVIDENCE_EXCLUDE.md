# 灵犀 SynergyOS · 用户排除要素优先级规则 · 真实验收证据（v0.1.1）

本文是 `EVIDENCE.md`（v0.1 四组证据）的**补充验证**：针对「用户显式排除要素」统一优先级规则，
用 **DeepSeek（deepseek-chat，OpenAI 兼容）** 真实模型端到端跑通，确认规则在真实数据流下有效。
所有产出均由 `python3 -m synergyos.cli` 真实调用大模型生成，未做任何人工编辑。

## 复现方式

```bash
# 项目根 .env 写入 OPENAI_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL（DeepSeek）
# ① paas：只列本周完成+进行中，排除风险/下周
python3 -m synergyos.cli --auto --scenario paas \
  --task "帮我写一份极简周报，只列本周完成和进行中两件事就够了，不要写风险和下周计划" \
  --emit out_paas_exclude --report --report-out reports_paas_exclude --report-format both

# ② biz：分析销售额，排除图表
python3 -m synergyos.cli --auto --scenario biz \
  --task "分析一下这周销售额，上周50万这周62万，涨了24，帮我出个简单分析就行，不用画" \
  --emit out_biz_exclude --report --report-out reports_biz_exclude --report-format both
```

## 证据结论

### 1. paas · 用户排除风险/下周 → 验收尊重用户，标注「已按用户要求省略」

- 任务：`只列本周完成和进行中，不要写风险和下周计划`
- 真实模型产出：周报仅含「本周完成」「进行中」两段，并写明「未包含风险与下周计划」。
- 验收输出：
  ```
  🔍 真实验证（结构化验收）：✅ 通过（一次通过，共运行 1 次）
     ⏭ 已按用户要求省略：风险/阻塞/问题/风险与阻塞；下周重点/下周计划/下周安排/下周（用户显式排除，不强制补全）
  ```
- 结论：**验收未强制补全用户排除的要素**，且不被「未包含风险」文字游戏骗成「已覆盖」。

### 2. biz · 用户排除图表 → 仅跳过图表，不误判数据

- 任务：`分析销售额…不用画`
- 验收输出：
  ```
  🔍 真实验证（结构化验收）：✅ 通过（一次通过，共运行 1 次）
     ⏭ 已按用户要求省略：图表/可视化/chart/柱状图/折线图/饼图/plot/render（用户显式排除，不强制补全）
  ```
- 结论：**只有图表组被跳过**；「数据/销售额」是任务天然核心，未被误判为排除项
  （修复前因 `数据` 组 exclude 词含「销售额/分析」会误触发，已修正）。

## 验证过程中发现并修复的真实缺陷（单测/真实验收共同暴露）

1. **文字游戏骗过 token 匹配**：模型写「未包含风险与下周计划」让风险/下周被误判为「已覆盖」，
   从而跳过 user-excluded 判定。→ 改为「user-excluded 判定优先于覆盖判定」。
2. **biz「数据」组 exclude 词过宽**：exclude 含任务高频词「销售额/分析」，叠加强排除信号
   「不用画」误判数据组为排除项。→ biz「数据」组 `verify_excludes=[]`（不参与排除检测）；
   未配置/空 exclude 语义改为「该组永不可被排除」。
3. **「只列 A B」语义歧义**：`只列本周完成和进行中` 中的 A、B 是被点名包含，不应归 skipped。
   → 拆分「限定包含（只列/只要）」与「强排除（不要/不用）」：限定包含点名的要素视为包含，
   未被点名的其余要素才归 skipped（用「限定信号词之后、首个强排除信号之前」的 zone 解析）。

## 测试

`tests/test_core.py` 新增 4 项针对排除规则的用例（文字游戏、无信号不触发、biz 仅跳过图表、
paas 限定包含点名），全量 **48 项单测通过**。
